import React from 'react';
import Contenedor from '../components/Contenedor';

const Agenda = () => {
    return (
        <Contenedor>
            agenda menú
        </Contenedor>
    );
};

export default Agenda;